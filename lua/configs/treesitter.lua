local r = {
  ensure_installed = { "c", "lua", "vim", "vimdoc", "query", "markdown", "markdown_inline", "yaml" },
  auto_install = true,
  highlight = {
    enable = true,
  },
  indent = { enable = false },
}
return r
